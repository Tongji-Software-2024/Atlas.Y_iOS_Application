import SwiftUI

struct DynamicHomeView: View {
    var body: some View {
        VStack {
            Text("DynamicHomeView")
        }
        .frame(maxWidth: 1000)
    }
}

#Preview {
    DynamicHomeView()
}
