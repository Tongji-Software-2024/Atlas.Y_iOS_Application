import SwiftUI

struct RegisterView: View {
    var body: some View {
        VStack {
            Text("RegisterView")
        }
        .frame(maxWidth: 1000)
    }
}

#Preview {
    RegisterView()
}
