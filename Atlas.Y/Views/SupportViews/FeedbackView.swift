import SwiftUI

struct FeedbackView: View {
    var body: some View {
        VStack {
            Text("FeedbackView")
        }
        .frame(maxWidth: 1000)
    }
}

#Preview {
    FeedbackView()
}
