import SwiftUI

struct AlgorithmStoreView: View {
    var body: some View {
        VStack {
            Text("AlgorithmStoreView")
        }
        .frame(maxWidth: 1000)
    }
}

#Preview {
    AlgorithmStoreView()
}
