import SwiftUI

struct AlgorithmStoreView: View {
    var body: some View {
        VStack {
            Text("AlgorithmStoreView")
        }
        .padding()
    }
}

#Preview {
    AlgorithmStoreView()
}
