import SwiftUI

struct WebView: View {
    var body: some View {
        VStack {
            Text("WebView")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color.white)
    }
}

#Preview {
    WebView()
}
