import SwiftUI

struct ContactUsView: View {
    var body: some View {
        VStack {
            Text("ContactUsView")
        }
        .frame(maxWidth: 1000)
    }
}

#Preview {
    ContactUsView()
}
